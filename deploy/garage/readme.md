No need to do this, but if you want to explore the admin API do the following:

Run docs server:

```bash
cd /Users/mpw/repos/garage/doc/api
python3 -m http.server
```

Open in browser
`http://localhost:8000/garage-admin-v1.html`
