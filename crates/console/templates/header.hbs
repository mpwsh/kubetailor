<!DOCTYPE html>
<html lang="en">
<head>
  {{> meta}} 
  {{> links}}
  {{> scripts}} 
</head>
<body>
  {{#if big_container}}
    <div class="full-height-logs-flex-container">
      <div class="full-height-logs-form-container">
  {{else}}
    {{#if action}} 
    <div class="full-height-flex-container">
      <div class="full-height-form-container">
    {{else}}
    <div class="flex-container">
      <div class="form-container">
    {{/if}}
  {{/if}}
